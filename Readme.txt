В задании есть неточности:
"В целях упрощения реализации - все задания поступают в лифт до начала его движения."
и
"Все задачи поступают в модуль управления лифта последовательно и не прерывают текущую инструкцию выполняемую модулем.
Новые задачи могут поступать в блок управления с любых вызывных панелей только при остановке лифта на любом из этажей."
Возможно я не нравильно понял - но они противоречивы.

Также не понимаю как реализовать кнопку Stop, если все команды поступают одновременно.

Если мне уточнят необходимую реализацию, я внесу изменения.

Тесты запускаются командой:
mvn test
при наличии:
jre > 1.8 и прописаного JAVA_HOME в переменных среды
и maven 3 и прописаного MAVEN_HOME в переменных среды